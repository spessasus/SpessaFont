import type { AudioEngine } from "../core_backend/audio_engine.ts";

export function Settings({ engine }: { engine: AudioEngine }) {
    return <div className={"settings"}>SETTINGS WILL GO HERE</div>;
}
