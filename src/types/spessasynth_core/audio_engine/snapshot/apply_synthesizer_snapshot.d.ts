/**
 * Applies the snapshot to the synth
 * @param snapshot {SynthesizerSnapshot}
 * @this {SpessaSynthProcessor}
 */
export function applySynthesizerSnapshot(this: SpessaSynthProcessor, snapshot: SynthesizerSnapshot): void;
export class applySynthesizerSnapshot {
    /**
     * Applies the snapshot to the synth
     * @param snapshot {SynthesizerSnapshot}
     * @this {SpessaSynthProcessor}
     */
    constructor(this: SpessaSynthProcessor, snapshot: SynthesizerSnapshot);
    _snapshot: SynthesizerSnapshot;
}
import { SynthesizerSnapshot } from "./synthesizer_snapshot.js";
